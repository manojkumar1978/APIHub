openapi: 3.0.1
info:
  title: smartRoutingAPI
  description: API Definition of smartRoutingAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/smart
paths:
  /routing:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
